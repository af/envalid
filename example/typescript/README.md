# Envalid and TypeScript

A simple example of using Envalid with TypeScript.

Run

```
yarn install
yarn start
```

or

```
npm install
npm start
```

You can also run `yarn test` (or `npm test`) to verify that TypeScript
type-checking passes.

